{
  "name": "@payclear/program",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "command -v anchor >/dev/null 2>&1 && anchor build || cargo check",
    "test": "ts-mocha -p ./tsconfig.json -t 120000 tests/payclear.ts",
    "clean": "command -v anchor >/dev/null 2>&1 && anchor clean || cargo clean"
  },
  "devDependencies": {
    "@coral-xyz/anchor": "^0.30.1",
    "@solana/spl-token": "^0.4.6",
    "@solana/web3.js": "^1.95.0",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^10.0.0",
    "chai": "^4.4.0",
    "mocha": "^10.7.0",
    "ts-mocha": "^10.0.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
